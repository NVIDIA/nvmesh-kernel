# PET Controller Initialization and Propagation Sequence Diagrams

This document describes the initialization and propagation flow of `nvmeib_pet_base_controller` derived structures through the NVMesh system.

This document was generated by AI and reviewed by human. The request to the cursor was:
1. read "pet.md" and "nvmeib_pet_specification.h" files
2. draw the call diagrams, using markdown, where nvmeib_pet_base_controller derived struct instance is initialized and used

Feel free to regenerate the file yourself.

## Overview

The PET (Per Entity Tracing) controller follows a hierarchical initialization and propagation pattern:
1. **Common Module** creates the proc filesystem infrastructure
2. **nvmeibc Module** creates the concrete controller instance
3. **Control API** stores the controller reference
4. **Volumes** receive the controller during attachment
5. **Block Devices** receive the controller from volumes during initialization

---

## Sequence Diagram 1: Controller Initialization

This diagram shows how the PET controller is initialized during module loading.

```mermaid
sequenceDiagram
    participant Kernel
    participant CommonModule as Common Module<br/>(nvmeib)
    participant ProcFS as /proc/nvmeib
    participant NvmeibcModule as nvmeibc Module
    participant IoPetController as io_pet_controller<br/>(struct)
    participant ControlAPI as nvmeibc_control_api

    Note over Kernel,ControlAPI: Module Initialization Phase

    Kernel->>CommonModule: module_init()<br/>nvmeib_module_init()
    CommonModule->>CommonModule: procs_create()
    CommonModule->>ProcFS: proc_mkdir("io.pet", proc_dir)
    ProcFS-->>CommonModule: io_pet_dir
    CommonModule->>CommonModule: nvmeib_msgloop_create("io.pet", io_pet_dir)
    CommonModule->>CommonModule: Store in io_pet_writer
    Note over CommonModule: Export nvmeib_trace_get_io_pet_msgloop()

    Kernel->>NvmeibcModule: module_init()<br/>nvmeibc_init()
    NvmeibcModule->>NvmeibcModule: nvmeibc_instance_procs_create()
    NvmeibcModule->>NvmeibcModule: per_clnt_inst_proc_files_create()
    NvmeibcModule->>ControlAPI: nvmeibc_cc_api_create()
    
    ControlAPI->>NvmeibcModule: nvmeibc_io_pet_controller_create()
    NvmeibcModule->>CommonModule: nvmeib_trace_get_io_pet_msgloop()
    CommonModule-->>NvmeibcModule: io_pet_writer
    NvmeibcModule->>IoPetController: kzalloc(sizeof(struct io_pet_controller))
    NvmeibcModule->>IoPetController: Initialize base.flush<br/>Initialize base.get_buffer<br/>Initialize base.put_buffer<br/>Store io_pet_writer
    IoPetController-->>NvmeibcModule: &io_pet_controller.base
    NvmeibcModule-->>ControlAPI: Store in capi->io_pet_controller
```

---

## Sequence Diagram 2: Controller Propagation to Volumes

This diagram shows how the controller is propagated from the control API to volumes during volume attachment.

```mermaid
sequenceDiagram
    participant Management as Management<br/>(via MCS)
    participant ControlAPI as nvmeibc_control_api
    participant Volume as nvmeibc_volume
    participant VolumeAttach as nvmeibc_volume_attach()

    Note over Management,VolumeAttach: Volume Attachment Phase

    Management->>ControlAPI: Volume attach message<br/>(via MCS protocol)
    ControlAPI->>VolumeAttach: nvmeibc_volume_attach(p, msg, io_pet_controller)
    Note over VolumeAttach: io_pet_controller from<br/>capi->io_pet_controller
    
    VolumeAttach->>VolumeAttach: Check if volume exists<br/>(by UUID or name)
    alt Volume does not exist
        VolumeAttach->>Volume: kzalloc(sizeof(*volume))
        VolumeAttach->>Volume: volume->p = p
        VolumeAttach->>Volume: volume->io_pet_controller = io_pet_controller
        Note over Volume: Controller reference stored
        VolumeAttach->>VolumeAttach: nvmeibc_volume_header_create_from_msg()
        VolumeAttach->>VolumeAttach: nvmeibc_add_volume(volume)
    else Volume exists
        VolumeAttach->>VolumeAttach: nvmeibc_volume_update(volume)
    end
```

---

## Sequence Diagram 3: Controller Propagation to Block Devices

This diagram shows how the controller is propagated from volumes to block devices during block device initialization.

```mermaid
sequenceDiagram
    participant Volume as nvmeibc_volume
    participant BlockInit as nvmeibc_block_init()
    participant BlockDevice as nvmeibc_block_device
    participant Topology as Topology System

    Note over Volume,Topology: Block Device Initialization Phase

    Volume->>BlockInit: nvmeibc_block_init(conf, volume)
    Note over BlockInit: volume->io_pet_controller<br/>is available
    
    BlockInit->>BlockDevice: kzalloc(sizeof(*dev))
    BlockInit->>BlockDevice: Initialize device fields
    BlockInit->>BlockDevice: dev->io_pet_controller = volume->io_pet_controller
    Note over BlockDevice: Controller reference stored
    
    BlockInit->>Topology: nvmeibc_topologies_init()
    BlockInit->>Topology: nvmeibc_block___conf()
    BlockInit->>Topology: __bdev_datapath_init()
    BlockInit->>Volume: nvmeibc_volume_set_block_device(volume, dev)
    BlockInit->>Topology: nvmeibc_topologies_register()
```

---

## Sequence Diagram 4: Complete Initialization and Propagation Flow

This diagram shows the complete end-to-end flow from module initialization to block device usage.

```mermaid
sequenceDiagram
    participant Kernel
    participant CommonModule as Common Module
    participant NvmeibcModule as nvmeibc Module
    participant ControlAPI as Control API
    participant Volume as Volume
    participant BlockDevice as Block Device
    participant IOPath as I/O Path

    Note over Kernel,IOPath: Complete Initialization Flow

    rect rgb(200, 220, 255)
        Note over Kernel,CommonModule: Phase 1: Infrastructure Setup
        Kernel->>CommonModule: Module init
        CommonModule->>CommonModule: Create /proc/nvmeib/io.pet/
        CommonModule->>CommonModule: Create io_pet_writer msgloop
    end

    rect rgb(220, 255, 220)
        Note over NvmeibcModule,ControlAPI: Phase 2: Controller Creation
        Kernel->>NvmeibcModule: Module init
        NvmeibcModule->>ControlAPI: Create control API
        ControlAPI->>NvmeibcModule: Request controller creation
        NvmeibcModule->>CommonModule: Get io_pet_writer
        NvmeibcModule->>NvmeibcModule: Create io_pet_controller struct
        NvmeibcModule-->>ControlAPI: Store controller reference
    end

    rect rgb(255, 255, 200)
        Note over ControlAPI,Volume: Phase 3: Volume Attachment
        ControlAPI->>Volume: Volume attach (with controller)
        Volume->>Volume: Store controller reference
    end

    rect rgb(255, 220, 200)
        Note over Volume,BlockDevice: Phase 4: Block Device Init
        Volume->>BlockDevice: Block device init
        BlockDevice->>BlockDevice: Store controller reference
    end

    rect rgb(240, 240, 240)
        Note over BlockDevice,IOPath: Phase 5: Runtime Usage
        IOPath->>BlockDevice: I/O operation
        BlockDevice->>BlockDevice: Use io_pet_controller<br/>for tracing
    end
```

---

## Key Data Structures

### Controller Hierarchy

```
struct nvmeib_pet_base_controller (interface)
    ├── flush()
    ├── get_buffer()
    └── put_buffer()

struct io_pet_controller (kernel implementation)
    ├── base: struct nvmeib_pet_base_controller
    ├── writer: struct msgloop_procfs_ent*
    └── cfg: { pet_buffer_size, msg_allocation_size }
```

### Storage Locations

1. **Common Module**: `io_pet_writer` (static variable)
2. **Control API**: `capi->io_pet_controller` (pointer)
3. **Volume**: `volume->io_pet_controller` (pointer)
4. **Block Device**: `dev->io_pet_controller` (pointer)

---

## Notes

- The controller is created once per nvmeibc module instance
- All volumes and block devices share the same controller instance
- The controller uses the msgloop infrastructure created by the common module
- Runtime journal layout and rotation are described in `pet.md`. Controller
  propagation is independent from whether
  an entity is still appending linearly or has rotated its suffix.
- Controller creation can fail if:
  - Memory allocation fails
  - Buffer size configuration is invalid (< 256 bytes)
  - The io_pet_writer from common module is unavailable

